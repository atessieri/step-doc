=====================
STEP Connection Guide
=====================

This guide explain how to use the STEP Library to communicate to STEP broker using both asynchronous 
(Coroutine) and synchronous (Non-blocking) approaches.

-------------
Prerequisites
-------------

Before using STEP Library, ensure you have:

- Python 3.11 or higher
- STEP Library installed
- Valid STEP broker credentials

--------------
Basic Concepts
--------------

The main features of the STEP Library include:

    #. Publish ITS Etsi messages to the broker (STEP).
    #. Subscribe to the specific topics in order to receive ITS Etsi messages.

The library supports CAM, CPM and DENM messages.

The library provides two main approaches for MQTT communication:

    #. Synchronous (Non-blocking) using ``SyncComManager``
    #. Asynchronous (Coroutine) using ``AsyncComManager``

It is recommended to provide a *cache* folder during the initialization of the library
for performance reason. 
This folder is used to store the ASN.1 information used during the parsing of the messages.
No any part of the message is stored in the cache folder.

The information in the message is encapsulated and passed to the library using:

    #. The class
    #. A dictionary
    #. A JSON string

The communication session is managed with or without the context manager. 

-----------------------
Publish receive Example
-----------------------

The following example explains how to publish a CAM message using the STEP Library.

-----------------
Synchronous Usage
-----------------

The synchronous *Non-blocking* approach uses an transfer thread that manages the communication.
The library create a message FIFO queue to avoid any block due to transmission / reception delay.

Configuration
-------------

The following snippet explains how to create the data-class of parameters.

.. code-block:: python

    import logging
    import ssl

    from aiomqtt import TLSParameters
    from step_cloud_schemas import MessageId
    from step_lib import MqttQoS, PublishTopicParams, SyncComManagerParams

    def create_sync_com_manager_params() -> SyncComManagerParams:
        tls_params = TLSParameters(
            ca_certs=None,
            certfile=None,
            keyfile=None,
            cert_reqs=ssl.VerifyMode.CERT_REQUIRED,
            tls_version=ssl.PROTOCOL_TLSv1_2,
            ciphers=None,
            keyfile_password=None,
        )
        return SyncComManagerParams(
            instance_name="step_lib_test_1",
            host="step.example.com",
            port=8883,
            username="your_username",
            password="your_password",
            station_id=123,
            timeout=10,
            keepalive=30,
            bind_address="",
            bind_port=0,
            clean_start=True,
            queue_type=None,
            max_queued_incoming_messages=60,
            max_queued_outgoing_messages=60,
            max_inflight_messages=60,
            max_concurrent_outgoing_calls=60,
            tls_context=None,
            tls_params=tls_params,
            proxy=None,
            socket_options=None,
            session_expiry=0,
            receive_maximum=10,
            maximum_packet_size=4096,
            topic_alias_maximum=0,
            publish_topics_params=[
                PublishTopicParams(
                    message_type=MessageId.CAM_PDU,
                    header_topic="v2x/cam/264421_4",
                    geohash_precision=8,
                    qos=MqttQoS.QOS_0,
                ),
                PublishTopicParams(
                    message_type=MessageId.CPM_PDU,
                    header_topic="v2x/cpm/264421_4",
                    geohash_precision=8,
                    qos=MqttQoS.QOS_0,
                ),
            ],
            cache_dir="./.cache",
            reconnect_interval=2,
            max_incoming_queue_rooms=60,
            max_outgoing_queue_rooms=60,
            publish_worker_count=2,
            publish_worker_startup_timeout=15.0,
            publish_hwm=2000,
            publish_health_check_interval=1.0,
            publish_heartbeat_interval=0.5,
            publish_heartbeat_timeout=3.0,
            publish_parent_watchdog_interval=1.0,
            publish_graceful_shutdown_timeout=5.0,
            publish_forceful_join_timeout=5.0,
            publish_max_connect_attempts=5,
        )

.. note::

    The ``station_id`` field can be None if the library is able to send messages created
    by different stations. If ``station_id`` is None, it isn't used; otherwise, the
    library filters received messages to those matching ``station_id`` and overrides
    the station id in published messages with it.

.. note::

    ``SyncComManager`` publishes through a pool of worker processes rather than
    on the calling thread. The ``publish_worker_*`` and ``publish_*`` fields tune
    that pool:

        - ``publish_worker_count``: number of publish worker processes to spawn.
        - ``publish_worker_startup_timeout``: seconds to wait for a spawned worker
          to signal readiness before treating it as failed.
        - ``publish_hwm``: high-water mark for the worker pool's ZMQ PUSH socket.
        - ``publish_health_check_interval``: seconds between periodic worker
          health checks.
        - ``publish_heartbeat_interval``: seconds between worker heartbeat
          refreshes.
        - ``publish_heartbeat_timeout``: seconds of heartbeat staleness after
          which a worker is considered blocked.
        - ``publish_parent_watchdog_interval``: seconds between a worker's checks
          that its parent process is still alive.
        - ``publish_graceful_shutdown_timeout``: seconds to wait for workers to
          exit gracefully during ``close()`` before escalating to a forceful
          termination. If this manager runs inside a container/service manager,
          keep it (plus ``publish_forceful_join_timeout``) safely below that
          manager's own outer shutdown grace period.
        - ``publish_forceful_join_timeout``: seconds to wait for a
          forcefully-terminated worker to exit before escalating further.
        - ``publish_max_connect_attempts``: consecutive failed MQTT connect
          attempts a worker tolerates before giving up and exiting, to be picked
          up and restarted by the pool's health check, rather than retrying
          forever.

Using Context Manager (Recommended)
-----------------------------------

The context manager can manage resource cleanup automatically. So is recommended.
The following snippet of code:
    
    #. Creates a SyncComManager instance (client) managed by Context Manager
    #. Subscribes to the topics
    #. Publishes CAM and CPM messages
    #. Processes the received messages in the queue

.. code-block:: python

    from step_cloud_schemas import ItsTime, TrafficParticipantType
    from step_lib import (
        CamMessageV1,
        CpmMessageV1,
        GeoNetworkAddress,
        GpsLocation,
        MqttQoS,
        SyncComManager,
    )

    # Use context manager for automatic resource cleanup
    params = create_sync_com_manager_params()
    with SyncComManager(params, logger=logger) as client:

        # Subscribe to the topics
        client.subscribe(
            topics=[
                "v2x/cam/264421_4/g8/+/+/+/+/#",
                "v2x/cpm/264421_4/g8/+/+/+/+/#",
                "v2x/denm/264421_4/g8/+/+/+/+/#",
            ],
            max_qos=MqttQoS.QOS_1,
            no_local=True,
        )

        # Address and current position required by publish_its_message
        current_position = GpsLocation(
            time=ItsTime(),
            latitude=50.123456,
            longitude=8.123456,
            accuracy=None,
            speed=None,
            heading=None,
        )
        address = GeoNetworkAddress(
            is_manual_configured=True,
            traffic_participant_type=TrafficParticipantType.PASSENGER_CAR,
            mac_id=123,
        )

        # Publish CAM message
        cam_message = CamMessageV1()  # Create your CAM message
        client.publish_its_message(
            message=cam_message,
            address=address,
            current_position=current_position,
            is_station_mobile=True,
        )

        # Publish CPM message
        cpm_message = CpmMessageV1()  # Create your CPM message
        client.publish_its_message(
            message=cpm_message,
            address=address,
            current_position=current_position,
            is_station_mobile=True,
        )

        # Process received messages
        for message in client.its_messages_received:
            print(f"Received message: {message}")

.. note::

    The ``no_local`` field can be:

        - True: The client will not receive messages that it has published.
        - False: The client will receive messages that it has published.

    If the ``no_local`` field is True, the client isn't able to use shared subscription.

Manual management
-----------------

The following snippet of code:

    #. Creates a SyncComManager instance (client)
    #. Subscribes to the topics
    #. Publishes CAM and CPM messages
    #. Processes the received messages in the queue

.. code-block:: python

    client = None
    try:
        params = create_sync_com_manager_params()
        client = SyncComManager(params, logger=logger)

        # Start the client
        client.start()

        # Subscribe to the topics
        client.subscribe(
            topics=[
                "v2x/cam/264421_4/g8/+/+/+/+/#",
                "v2x/cpm/264421_4/g8/+/+/+/+/#",
                "v2x/denm/264421_4/g8/+/+/+/+/#",
            ],
            max_qos=MqttQoS.QOS_1,
            no_local=True,
        )

        # Address and current position required by publish_its_message
        current_position = GpsLocation(
            time=ItsTime(),
            latitude=50.123456,
            longitude=8.123456,
            accuracy=None,
            speed=None,
            heading=None,
        )
        address = GeoNetworkAddress(
            is_manual_configured=True,
            traffic_participant_type=TrafficParticipantType.PASSENGER_CAR,
            mac_id=123,
        )

        # Publish CAM message
        cam_message = CamMessageV1()  # Create your CAM message
        client.publish_its_message(
            message=cam_message,
            address=address,
            current_position=current_position,
            is_station_mobile=True,
        )

        # Publish CPM message
        cpm_message = CpmMessageV1()  # Create your CPM message
        client.publish_its_message(
            message=cpm_message,
            address=address,
            current_position=current_position,
            is_station_mobile=True,
        )

        # Process received messages
        for message in client.its_messages_received:
            print(f"Received message: {message}")

    finally:
        if client:
            client.stop()
    
.. note::
    
    The ``finally`` is necessary to ensure that the client is stopped and resources 
    are released with or without exception thrown. 

Receive Example
---------------

The function to receive messages is Non-blocking and can be used in a loop without 
timing penalty. The subscription is done during the initialization of the library.
The following example demonstrates how to receive a message using the STEP Library.

.. code-block:: python

    for message in client.its_messages_received:
        logger.info(msg=f"Message received: {message}")

For more detailed information about specific message types and configurations,
refer to the API Reference section :ref:`step_lib_api`.

------------------
Asynchronous Usage
------------------

Asynchronous approach uses the coroutine feature (*asyncio* library in python).
The *asyncio* module provides tools for creating and managing asynchronous task and 
for developing non-blocking network communication.
In the asynchronous approach, only the communication using context manager is supported.

Configuration
-------------

The following snippet explains how to create the data-class of parameters and use
it to build a ready-to-use ``AsyncComManager`` instance.

.. code-block:: python

    import logging
    import ssl

    from aiomqtt import TLSParameters
    from step_cloud_schemas import MessageId
    from step_lib import AsyncComManager, AsyncComManagerParams, MqttQoS, PublishTopicParams

    def create_async_com_manager(logger: logging.Logger) -> AsyncComManager:
        tls_params = TLSParameters(
            ca_certs=None,
            certfile=None,
            keyfile=None,
            cert_reqs=ssl.VerifyMode.CERT_REQUIRED,
            tls_version=ssl.PROTOCOL_TLSv1_2,
            ciphers=None,
            keyfile_password=None,
        )
        params = AsyncComManagerParams(
            instance_name="step_lib_test_1",
            host="step.example.com",
            port=8883,
            username="your_username",
            password="your_password",
            station_id=123,
            timeout=10,
            keepalive=30,
            bind_address="",
            bind_port=0,
            clean_start=True,
            queue_type=None,
            max_queued_incoming_messages=60,
            max_queued_outgoing_messages=60,
            max_inflight_messages=60,
            max_concurrent_outgoing_calls=60,
            tls_context=None,
            tls_params=tls_params,
            proxy=None,
            socket_options=None,
            session_expiry=0,
            receive_maximum=10,
            maximum_packet_size=4096,
            topic_alias_maximum=0,
            publish_topics_params=[
                PublishTopicParams(
                    message_type=MessageId.CAM_PDU,
                    header_topic="v2x/cam/264421_4",
                    geohash_precision=8,
                    qos=MqttQoS.QOS_0,
                ),
                PublishTopicParams(
                    message_type=MessageId.CPM_PDU,
                    header_topic="v2x/cpm/264421_4",
                    geohash_precision=8,
                    qos=MqttQoS.QOS_0,
                ),
            ],
            cache_dir="./.cache",
        )
        return AsyncComManager(params=params, logger=logger)

.. note::

    The ``station_id`` field can be None if the library is able to send messages created
    by different stations. If ``station_id`` is None, it isn't used; otherwise, the
    library filters received messages to those matching ``station_id`` and overrides
    the station id in published messages with it.

Using Context Manager
-----------------------------------

The context manager can manage resource cleanup automatically.
The following snippet of code:
    
    #. Creates an AsyncComManager instance (client) managed by Context Manager
    #. Subscribes to the topics
    #. Publishes CAM, CPM and DENM messages
    #. Processes the received messages in the queue

.. code-block:: python

    from asyncio import TaskGroup
    from asyncio import sleep as asyncio_sleep

    from paho.mqtt.subscribeoptions import SubscribeOptions
    from step_cloud_schemas import ItsTime, TrafficParticipantType
    from step_lib import GeoNetworkAddress, GpsLocation, MqttQoS

    # create_async_com_manager is the function defined above; receive_messages,
    # create_cam_orm_message, create_cpm_orm_message, and create_denm_orm_message
    # are application-defined helpers (not part of step_lib)

    # Use context manager for automatic resource cleanup
    async with create_async_com_manager(logger=logger) as client:
        topics = [
            "v2x/cam/264421_4/g8/+/+/+/+/#",
            "v2x/cpm/264421_4/g8/+/+/+/+/#",
            "v2x/denm/264421_4/g8/+/+/+/+/#",
        ]
        sub_results = await client.subscribe(
            topic=[
                (
                    topic,
                    SubscribeOptions(
                        qos=MqttQoS.QOS_1.value,
                        noLocal=False,
                        retainAsPublished=False,
                        retainHandling=SubscribeOptions.RETAIN_DO_NOT_SEND,
                    ),
                )
                for topic in topics
            ]
        )
        for topic, result in zip(
            topics,
            sub_results,
        ):
            if result == MqttQoS.QOS_1.value:
                logger.info(msg=f"Subscribed to {topic}: {result}")
            else:
                logger.error(msg=f"Error subscribing to {topic}: {result}")
        # Address and current position required by publish_its_message
        current_position = GpsLocation(
            time=ItsTime(),
            latitude=50.123456,
            longitude=8.123456,
            accuracy=None,
            speed=None,
            heading=None,
        )
        address = GeoNetworkAddress(
            is_manual_configured=True,
            traffic_participant_type=TrafficParticipantType.PASSENGER_CAR,
            mac_id=123,
        )
        async with TaskGroup() as tg:
            # Receive message task
            receive_message_task = tg.create_task(
                coro=receive_messages(client=client, logger=logger)
            )
            # CAM message as ORM
            await client.publish_its_message(
                message=create_cam_orm_message(),
                address=address,
                current_position=current_position,
                is_station_mobile=True,
            )
            # CPM message as ORM
            await client.publish_its_message(
                message=create_cpm_orm_message(),
                address=address,
                current_position=current_position,
                is_station_mobile=True,
            )
            # DENM message as ORM
            await client.publish_its_message(
                message=create_denm_orm_message(),
                address=address,
                current_position=current_position,
                is_station_mobile=True,
            )
            # Simulate other operations
            await asyncio_sleep(delay=2)
            # Cancel receive message task
            receive_message_task.cancel()

.. note::

    The ``no_local`` field can be:

        - True: The client will not receive messages that it has published.
        - False: The client will receive messages that it has published.
    
    If the ``no_local`` field is True, the client isn't able to use shared subscription.

.. note::
    
    The ``TaskGroup`` is used to manage the receive tasks and ensure that all tasks are 
    completed before the context manager is closed.

Receive Example
---------------

The function to receive messages is a infinite loop stopped by *coroutine* mechanism
during the message waiting.   
So this loop is executed in a coroutine concurrently with other tasks.
The following example explains how to receive a message using the STEP Library.

.. code-block:: python

    async for message in client.its_messages:
        orm_message: FacilityMessage = message.its_payload
        logger.info(
            msg=f"Message(qos:{message.qos},topic:{message.topic}): {orm_message}",
        )


For more detailed information about specific message types and configurations,
refer to the API Reference section :ref:`step_lib_api`.


----------------
Complete Example
----------------

Following example demonstrates how to use the STEP Library to publish and receive messages using 
both synchronous and asynchronous approaches.

Asynchronous example:

.. literalinclude:: ../examples/step_publish/async_manager.py
  :language: Python

Synchronous example:

.. literalinclude:: ../examples/step_publish/sync_manager.py
  :language: Python