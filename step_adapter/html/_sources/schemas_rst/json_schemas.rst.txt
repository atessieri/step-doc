JSON Schemas
============

The STEP Adapter uses JSON schemas to define the structure of messages and configurations. 
This ensures that the data exchanged between the adapter and others adheres to a specific format, 
facilitating interoperability and validation.

Input Message Schema
--------------------

The schema defines the structure for messages incoming of STEP Adapter subsystem.

.. toctree::
   :maxdepth: -1

   cloud_events_step_adapter_schema

Output Message Schema
---------------------

The schema defines the structure for messages outgoing of STEP Adapter subsystem.

.. toctree::
   :maxdepth: -1

   cloud_events_step_crash_prediction_schema

ITS Message Schema
------------------

The messages supported by STEP Adapter are as follows:

   * **CAM**: Defines the structure for Cooperative Awareness Messages (CAM) used in the ITS system

.. toctree::
   :maxdepth: -1

   cam_message

Configuration Schema
--------------------

The schema defines the structure for configuration of STEP Adapter subsystem.

.. toctree::
   :maxdepth: -1

   step_adapter_config_schema
