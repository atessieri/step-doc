JSON Schemas
============

The STEP Crash Prediction uses JSON schemas to define the structure of messages and configurations. 
This ensures that the data exchanged between the Adapter and others adheres to a specific format, 
facilitating interoperability and validation.

Input Message Schema
--------------------

The schema defines the structure for messages incoming of STEP Crash Prediction subsystem.

.. toctree::
   :maxdepth: -1

   cloud_events_step_crash_prediction_schema

Output Message Schema
---------------------

The schema defines the structure for messages outgoing of STEP Crash Prediction subsystem.

.. toctree::
   :maxdepth: -1

   cloud_events_step_producer_schema

Configuration Schema
--------------------

The schema defines the structure for configuration of STEP Crash Prediction subsystem.

.. toctree::
   :maxdepth: -1

   step_crash_prediction_config_schema
