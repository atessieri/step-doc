JSON Schemas
============

The STEP Producer uses JSON schemas to define the structure of messages and configurations. 
This ensures that the data exchanged between the producer and others adheres to a specific format, 
facilitating interoperability and validation.

Input Message Schema
--------------------

The schema defines the structure for messages incoming of Step Producer subsystem.

.. toctree::
   :maxdepth: -1

   cloud_events_step_producer_schema

Output Message Schema
---------------------

The schema defines the structure for messages outgoing of Step Producer subsystem.

.. toctree::
   :maxdepth: -1

   cloud_events_step_client_schema

ITS Message Schema
------------------

The messages supported by Step Producer are as follows:

   * **DENM**: Defines the structure for Decentralized Environmental Notification 
     Messages (DENM) used in the ITS system

.. toctree::
   :maxdepth: -1

   denm_message

Configuration Schema
--------------------

The schema defines the structure for configuration of Step Producer subsystem.

.. toctree::
   :maxdepth: -1

   step_producer_config_schema
