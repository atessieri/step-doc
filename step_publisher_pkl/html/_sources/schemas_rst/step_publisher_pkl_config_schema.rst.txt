STEP Publisher Pickle Config V1 Schema
=======================================

The STEP Publisher Pickle Config Version 1 Schema defines a comprehensive configuration structure
for the STEP Publisher Pickle component. This JSON Schema is automatically generated from Pydantic
models and provides strong typing, validation rules, and documentation.

Overview
--------

The STEP Publisher Pickle Configuration is organized hierarchically with the main ``StepPublisherPklConfig``
class containing several nested configuration components for different aspects of the system:

  * **Base Configuration**

      * ``templateVersion``: The version of the template (currently "1")
      * ``pklFolderPath``: Path to the pickle folder for storing/reading pickle files

  * **STEP Broker Configuration**

      * MQTT Client configuration (connection parameters, security settings)
      * MQTT Protocol configuration (QoS, topic management, session parameters)
      * Cache directory configuration

  * **Logging Configuration**

      * Log handlers (stream, file, rotating file handlers)
      * Log levels
      * Formatting options

Features and Benefits
---------------------

* **Type Validation**: Strong typing and constraints via Pydantic fields
* **Immutable Design**: Configuration is frozen after loading to prevent runtime errors
* **Comprehensive Documentation**: Detailed field descriptions and examples
* **Default Values**: Sensible defaults provided for all configuration options
* **Strict Validation**: Prevents configuration errors before they cause runtime issues

Example
-------

This example is created using JSON Schema **version 1**. For further information, please refer 
to the python classes. 

Example of the configuration schema:

.. literalinclude:: ../schemas_examples/step_publisher_pkl_config_v1_schema_template.json
  :language: JSON

JSON Schema
-----------

.. _step_publisher_pkl_config_schema:

This is JSON Schema **version 1**

.. jsonschema:: ../json_schemas/step_publisher_pkl_config_v1_schema.json