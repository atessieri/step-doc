STEP Publisher Pickle: Overview
===============================

.. raw:: html

   <a class="back-to-portal" href="../../index.html">Back to STEP Documentation</a>

.. |DEV_STAT| image:: https://img.shields.io/badge/status-beta-yellow.svg
   :target: https://gitlab.automotive.vodafone.com/vfa-init/pnrr/step_publisher_pkl
   :alt: Development Status
.. |GOOGLE_STYLE| image:: https://img.shields.io/badge/%20style-google-3666d6.svg
   :target: https://google.github.io/styleguide/pyguide.html#s3.8-comments-and-docstrings
   :alt: Google Style
.. |ISORT| image:: https://img.shields.io/badge/%20imports-isort-%231674b1
   :target: https://pycqa.github.io/isort/
   :alt: Isort
.. |PYTHON_VERSION| image:: https://img.shields.io/badge/python-3.13%2B-blue.svg
   :target: https://www.python.org/downloads/
   :alt: Python Version
.. |UV| image:: https://img.shields.io/badge/package%20manager-uv-DE5FE9.svg
   :target: https://github.com/astral-sh/uv
   :alt: Uv
.. |PYTEST| image:: https://img.shields.io/badge/tests-pytest-orange.svg
   :target: https://docs.pytest.org/
   :alt: Pytest
.. |RUFF| image:: https://img.shields.io/endpoint?url=https://raw.githubusercontent.com/astral-sh/ruff/main/assets/badge/v2.json
   :target: https://github.com/astral-sh/ruff
   :alt: Ruff
.. |MYPY| image:: https://img.shields.io/badge/typing-mypy-blue.svg
   :target: https://mypy.readthedocs.io/
   :alt: Mypy
.. |PYLINT| image:: https://img.shields.io/badge/code%20quality-pylint-blue.svg
   :target: https://pylint.pycqa.org/
   :alt: Pylint
.. |SPHINX_DOCS| image:: https://img.shields.io/badge/docs-sphinx-blue.svg
   :target: https://www.sphinx-doc.org/en/master/index.html
   :alt: Sphinx
.. |TOX| image:: https://img.shields.io/badge/automated%20testing-tox-brightgreen.svg
   :target: https://tox.readthedocs.io/
   :alt: Tox
.. |TEST_COVERAGE| image:: ./badges/test_coverage.svg
.. |DOCSTRING_COVERAGE| image:: ./badges/docstr_coverage.svg

+----------------------+----------------------------------------------------------+
| **Code Status**      | |DEV_STAT| |DOCSTRING_COVERAGE| |TEST_COVERAGE|          |
+----------------------+----------------------------------------------------------+
| **Code Quality**     | |RUFF| |ISORT| |MYPY| |PYLINT|                           |
+----------------------+----------------------------------------------------------+
| **Documentation**    | |SPHINX_DOCS| |GOOGLE_STYLE|                             |
+----------------------+----------------------------------------------------------+
| **Testing**          | |PYTEST| |TOX|                                           |
+----------------------+----------------------------------------------------------+
| **Python**           | |PYTHON_VERSION| |UV|                                    |
+----------------------+----------------------------------------------------------+

.. list-table::
   :widths: 20 80
   :stub-columns: 1

   * - Version
     - |release|
   * - Release Date
     - |release_date|

.. toctree::
   :maxdepth: -1
   :hidden:
   :caption: STEP Publisher Pickle:

   project_creation
   installation
   commands
   usage
   input_files
   package_script
   architecture
   docstring_coverage_report
   coverage_report
   schemas_rst/json_schemas
   step_publisher_pkl_rst/step_publisher_pkl
   test_rst/test



The **STEP Publisher Pickle** is a specialized component of the STEP (Safer Transport for Europe Platform)
ecosystem, designed to integrate vehicle trajectory/ITS message data with real-world ITS
infrastructure. This MQTT-based publisher translates scenario data stored in JSONL, pickle, or GPX
files into Cooperative Awareness (CAM), Collective Perception (CPM), or Decentralized Environmental
Notification (DENM) messages for the STEP broker, enabling realistic vehicle trajectory
testing within the ITS ecosystem.

Purpose
-------

The **STEP Publisher Pickle** serves as a bridge between recorded/simulated trajectory data and the
STEP platform, publishing vehicle position data from JSONL, pickle, or GPX scenario files as ITS
ETSI messages. It loads vehicle trajectory/message data including GNSS positions, speed, heading,
and timestamp information, then publishes them as CAM, CPM, or DENM messages to the STEP MQTT
broker with realistic timing and multi-vehicle support.

Environment Variables
----------------------

MQTT broker connection secrets are kept out of the (persisted) JSON configuration file and
are instead read from the process environment at startup, optionally loaded from a ``.env``
file, via the ``MQTT_URL``, ``MQTT_PORT``, ``MQTT_USERNAME``, and ``MQTT_PASSWORD``
variables; the URL and port are required, while the username and password are optional. See
the :doc:`usage<usage>` section for the full variable reference and an example ``.env`` file.

Goal
----

The **STEP Publisher Pickle** exists so that developers, testers, and CI/CD pipelines can exercise
the STEP ecosystem's ITS message ingestion, MQTT broker, and downstream services with realistic,
repeatable vehicle traffic, without needing a live simulator run or real vehicles on the road.
It replays previously recorded trajectory/message scenarios (JSONL, pickle, or GPX files) as a
stream of ETSI-compliant CAM, CPM, or DENM messages, published to the STEP MQTT broker with the
same timing, multi-vehicle concurrency, and message structure a real deployment would produce.
This gives the rest of the STEP platform a lightweight, deterministic traffic generator for
integration testing and development.

Key Features
------------

Scenario Data Loading
~~~~~~~~~~~~~~~~~~~~~~

- **Pluggable File Loaders**: Auto-detects and loads scenario data from JSONL ITS messages,
  pickle actor/vehicle trajectories, pickle camera/CPM data, or GPX track files (see
  :doc:`input_files`)
- **Time Reference Mapping**: Establishes mapping between scenario timestamps and real-world
  time for synchronized playback of recorded vehicle movements
- **Multi-Vehicle Support**: Processes multiple vehicle trajectories/messages concurrently
- **Station Message Management**: Manages vehicle message data as time-ordered sorted queues
  for efficient sequential access and consumption

ITS Message Publishing
~~~~~~~~~~~~~~~~~~~~~~

- **ETSI Compliance**: Publishes Cooperative Awareness (CAM), Collective Perception (CPM), and
  Decentralized Environmental Notification (DENM) messages following ITS standards
- **Message Encoding**: Converts scenario data into structured CAM/CPM/DENM format with proper
  ITS message components (Identification, ItsTime, ReferencePosition, Speed, Heading)
- **GeoNetwork Addressing**: Configures proper addressing with traffic participant type and MAC ID
  for ITS message routing
- **Realistic Timing**: Maintains precise timing intervals between message updates using ItsTime
  calculations

MQTT Communication
~~~~~~~~~~~~~~~~~~

- **Secure Connection**: TLS/SSL connections to STEP MQTT broker with certificate authentication
- **Configurable QoS**: Supports Quality of Service levels (0, 1, 2) for reliable message delivery
- **Asynchronous Publishing**: Non-blocking async/await patterns for high-throughput message publishing
- **Topic Management**: Flexible topic configuration with geohash precision support

Configuration and Monitoring
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

- **Hierarchical Configuration**: Pydantic-based configuration models with MQTT client, protocol, 
  broker, logging, and pickle folder path settings
- **JSON Schema Support**: Configuration validation and template generation via command-line options
- **Comprehensive Logging**: UTC-based timestamps with multiple handlers (stream, file, rotating) 
  and automatic ZIP compression for rotated logs
- **Signal Handling**: Cross-platform graceful shutdown (Windows SIGBREAK, Unix SIGINT)

Core Components
---------------

Scenario Management
~~~~~~~~~~~~~~~~~~~

The ``Scenario`` class provides comprehensive management of vehicle trajectory/ITS message
data for publishing to the STEP broker. It serves as the main data loader and orchestrator
for multi-vehicle playback and handles:

  - Loading vehicle trajectory/ITS message data through a pluggable set of ``FileLoader``
    implementations (JSONL ITS messages, pickle actor/vehicle trajectories, pickle
    camera/CPM data, and GPX track files), auto-detected per input file
  - Establishing time reference mapping between scenario time and real-world time
  - Managing vehicle message data as sorted queues (using ``SortedList``) for efficient
    time-ordered sequential access
  - Converting raw trajectory/message data to structured CAM, CPM, or DENM messages with
    proper ITS components, depending on the loader and source data
  - Providing vehicle IDs and messages through generator methods
  - Maintaining separate message queues for each vehicle enabling concurrent publishing

Key responsibilities:
  - Managing vehicle data lifecycle from loading to consumption
  - Coordinating time reference for realistic playback timing
  - Providing ``pop_message()`` generator for sequential, time-ordered message consumption
  - Calculating next message times for maintaining realistic timing intervals

STEP Broker Client
~~~~~~~~~~~~~~~~~~~

The ``StepBroker`` class provides MQTT communication with the STEP broker for publishing
CAM, CPM, and DENM messages. Extending ``AsyncComManager``, it implements:

  - **Secure MQTT Connection**: TLS/SSL connections with certificate authentication
  - **Message Publication**: Publishing facility messages (CAM/CPM/DENM) via ``client_publish_its_message()`` method
  - **Message Encoding**: Converting ``CamMessageV1`` objects to ITS message format
  - **GeoNetwork Configuration**: Setting up addressing with traffic participant type and MAC ID
  - **Context Management**: Asynchronous resource handling through ``__aenter__`` and ``__aexit__``

Main Application
~~~~~~~~~~~~~~~~

The main application (``__main__``) orchestrates the asynchronous publishing workflow:

- **Command-Line Interface**: Provides CLI for version display, configuration schema/template 
  generation, and publisher execution
- **Task Orchestration**: Uses ``asyncio.TaskGroup`` for concurrent publisher tasks per vehicle
- **Exit Code Management**: Defines standard exit codes (OK, USAGE, ERROR, ABORTED) via ``ExitCode`` enum
- **Signal Handling**: Cross-platform signal handling (Windows SIGBREAK, Unix SIGINT)
- **Publisher Coordination**: The ``_publisher_run()`` coroutine manages individual vehicle 
  publishing with timing calculations and sleep intervals
- **Event Loop Management**: Windows uses ``SelectorEventLoop`` for compatibility

Configuration System
~~~~~~~~~~~~~~~~~~~~

The configuration system (``StepPublisherPklConfig``) offers:

  - **Hierarchical Configuration**: Multi-level settings including MQTT client, protocol, 
    broker, logging, and pickle folder path
  - **Type Safety**: Strong typing and validation via Pydantic models
  - **JSON Schema Generation**: Schema validation and template creation through CLI options
  - **Immutable Design**: Frozen configuration ensuring consistency throughout execution
  - **Default Configurations**: Pre-configured defaults via ``get_default()`` methods

Logging Infrastructure
~~~~~~~~~~~~~~~~~~~~~~

The logging system (``StepLogger``) provides:

  - **UTC Formatting**: ``UtcFormatter`` class using ``gmtime`` for consistent timestamps
  - **Multiple Handlers**: Stream, file, rotating file (size/time-based), and null handlers
  - **Log Compression**: Automatic ZIP compression via ``_rotator()`` function for rotated logs
  - **Configurable Levels**: Adjustable logging verbosity (DEBUG, INFO, WARNING, ERROR, CRITICAL)
  - **Structured Logging**: Comprehensive operation logging with vehicle ID, timing, and position data

Exception Handling
~~~~~~~~~~~~~~~~~~

The exception system provides:

  - **Base Exception**: ``StepPublisherPklError`` for all publisher-related errors
  - **CLI Exception**: ``StepCliError`` for command-line specific errors (argument parsing,
    configuration loading)
  - **Loader Exception**: ``LoaderError`` raised by a ``FileLoader`` when a file does not
    match, or cannot be parsed as, the format it handles; ``Scenario.load_data()`` catches
    it to try the next candidate loader
  - **Shutdown Exception**: ``ShutdownRequestedError`` used internally to unwind the
    publisher's ``TaskGroup`` once a graceful shutdown has been requested via a signal
  - **Logging Integration**: Proper exception logging with appropriate severity levels
  - **Default Configurations**: Pre-configured default instances for quick setup and development
  - **Actor Dataset Configuration**: Specialized configuration for timing parameters and 
    trajectory management

Logging Infrastructure
~~~~~~~~~~~~~~~~~~~~~~

The logging system (``StepLogger``) provides:

  - **Multiple Handlers**: Stream, file, rotating file, and null handlers for flexible output options
  - **UTC Formatting**: Standardized UTC timestamp formatting for consistent log entries
  - **Configurable Levels**: Adjustable logging levels for different environments (development, production)
  - **Structured Logging**: Comprehensive operation logging throughout the service lifecycle 
    with detailed processing metrics

Architecture
------------

Event-Driven Processing
~~~~~~~~~~~~~~~~~~~~~~~

The publisher follows an event-driven architecture pattern:

  #. **Configuration Loading**: Loads configuration from JSON file via command-line arguments
  #. **Scenario Loading**: Reads vehicle trajectory/ITS message data from the configured
     input files (JSONL, pickle actor, pickle camera, or GPX), auto-detected per file
  #. **Broker Connection**: Establishes secure MQTT connection to STEP broker with TLS/SSL
  #. **Task Creation**: Creates concurrent publisher tasks using ``asyncio.TaskGroup`` for
     each vehicle in the scenario
  #. **Message Publishing**: Publishes CAM, CPM, or DENM messages with timing based on ItsTime calculations
  #. **Timing Management**: Sleeps between position updates to maintain realistic intervals
  #. **Graceful Shutdown**: Handles interruption signals and performs proper resource cleanup

Asynchronous Operations
~~~~~~~~~~~~~~~~~~~~~~~

The architecture utilizes:

  - **Async/await**: Non-blocking operations for concurrent multi-vehicle publishing
  - **TaskGroup**: Concurrent execution of publisher tasks with automatic cancellation on error
  - **Context Managers**: Asynchronous context management for MQTT connection lifecycle
  - **Event Loop**: Windows uses ``SelectorEventLoop`` for optimal compatibility

Message Flow
~~~~~~~~~~~~

#. **Input File Loading**: Vehicle trajectory/ITS message data loaded from the configured
   input files via the pluggable ``FileLoader`` implementations (JSONL, pickle actor, pickle
   camera, GPX)
#. **Time Reference Establishment**: Scenario time mapped to real-world time for synchronized playback
#. **Vehicle Iteration**: For each vehicle, separate concurrent publisher task created
#. **Message Consumption**: Messages consumed in time order via the ``pop_message()`` generator
#. **Message Conversion**: Data wrapped in ``CamMessageV1``, ``CpmMessageV1``, or ``DenmMessageV1`` format
#. **MQTT Publishing**: Messages published to STEP broker via ``client_publish_its_message()``
#. **Timing Calculation**: Time differences calculated using ``ItsTime.time_difference()``
#. **Realistic Delays**: Sleep intervals maintain original CARLA scenario timing

Standards Compliance
--------------------

The STEP Publisher Pickle adheres to key ITS standards:

- **ETSI TS 103 900**: Cooperative Awareness Messages (CAM) format and encoding

Dependencies
------------

Core Dependencies
~~~~~~~~~~~~~~~~~

- **step_cloud_schemas**: ITS message schemas including CAM format, ItsTime for timing calculations, 
  and CloudEvents structures
- **step_lib**: Core library providing AsyncComManager, CamMessageV1, MQTT utilities (MqttQoS, 
  PublishTopicParams), GPS location, and GeoNetwork addressing
- **aiomqtt**: Asynchronous MQTT client library with TLS support for non-blocking publishing
- **paho-mqtt**: MQTT protocol implementation with subscription options and QoS controls
- **pydantic**: Configuration validation and schema generation with type safety
- **asyncio**: Asynchronous processing capabilities for concurrent vehicle publishing

Development Dependencies
~~~~~~~~~~~~~~~~~~~~~~~~

- **pytest**: Testing framework for unit and integration tests
- **ruff**: Code formatting and linting tools
- **mypy**: Static type checking for Python code
- **sphinx**: Documentation generation for code documentation

Deployment
----------

The STEP Publisher Pickle can be deployed as:

- **Command-Line Tool**: Direct execution via ``python -m step_publisher_pkl`` with configuration file
- **Standalone Application**: Independent process for publishing CARLA simulation data
- **Integration Component**: Part of larger CARLA-to-STEP testing infrastructure

Configuration options support various deployment scenarios:

- **Development**: Local testing with debug logging and simplified configuration
- **Testing**: CARLA simulation replay with realistic timing for ITS infrastructure validation
- **CI/CD Integration**: Automated testing scenarios with CARLA-generated test data

Usage Patterns
--------------

The publisher supports multiple usage patterns:

Publishing Mode
~~~~~~~~~~~~~~~

Run as a publisher for CARLA simulation data:

.. code-block:: bash

   step_publisher_pkl --instance step_publisher_pkl --config config.json

Configuration Generation
~~~~~~~~~~~~~~~~~~~~~~~~

Generate configuration templates and schemas:

.. code-block:: bash

   step_publisher_pkl --conf_template config_template.json
   step_publisher_pkl --conf_schema config_schema.json

Version Information
~~~~~~~~~~~~~~~~~~~

Display version information for the package and dependencies:

.. code-block:: bash

   step_publisher_pkl --version

Testing
-------

The project includes comprehensive tests covering:

- **Unit Tests**: Individual component functionality including scenario loading and time reference
- **Configuration Tests**: Validation of configuration schemas and default values
- **Integration Tests**: MQTT publishing and CAM message encoding workflows
- **Timing Tests**: Verification of realistic timing interval calculations

Getting Started
---------------

To begin using the STEP Publisher Pickle, refer to the :doc:`installation<installation>`
section and the :doc:`usage<usage>` section for detailed guidance.
